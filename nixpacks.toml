# pg8000 sof Python — tizim kutubxonasi kerak emas.
[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt",
]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT"
